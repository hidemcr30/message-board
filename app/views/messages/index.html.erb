
<div class="description">
  <P>メッセージボードです。編集・削除も可能ですのでお気軽に一言どうぞ！</P>
</div>

<div class="panel panel-info">
  <div class="panel-heading">
    ＜メッセージ一覧＞
  </div>

  <div class="panel-body">
    <div class="alt-table-responsive">
      <table class="table table-striped table-bordered">
        <thead>
          <tr>
            <th class="col-xs-3 col-ms-2 col-md-2 col-lg-2">投稿者</th>
            <th class="col-xs-1 col-ms-1 col-md-1 col-lg-1">年齢</th>
            <th class="col-xs-5 col-ms-7 col-md-7 col-lg-7">内容</th>
            <th class="col-xs-3 col-ms-2 col-md-2 col-lg-2">オプション</th>
          </tr>
        </thead>
        <tbody>
        <% @messages.each do |message| %>
          <tr>
            <th><span class="label label-info"><%= message.name %></span></th>
            <td><span class="message-body"><%= message.age %>歳</span></td>
            <td><span class="message-body"><%= message.body %></span></td>
            <td>
              <span class="label label-success"><%= link_to "edit" , edit_message_path(message) %></span>
              <span class="label label-danger"><%= link_to "delete" , message , method: :delete, data: { confirm: '削除してもよろしいですか？'} %></span>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<% #部分テンプレートを読み込んでいる %>
<%= render 'form' %>


